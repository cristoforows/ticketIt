{
  "name": "openrouter-fidelity",
  "version": "0.1.0",
  "private": true,
  "description": "M1.14 (#25) - fake OpenRouter HTTP fixture server plus the pinned @langchain/openrouter ChatOpenRouter adapter fidelity matrix (tool calls, URL citations, usage accounting, truncated/error streams) for feasibility experiment S4. See docs/evidence/m1/25-openrouter-fidelity.md.",
  "type": "module",
  "engines": {
    "node": "26.9.0"
  },
  "scripts": {
    "test": "node --import tsx --test \"test/**/*.test.ts\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "shared": "file:../shared",
    "@langchain/core": "1.2.11",
    "@langchain/openrouter": "0.4.13"
  },
  "devDependencies": {
    "@types/node": "26.6.1",
    "tsx": "4.23.13",
    "typescript": "7.0.2"
  }
}
